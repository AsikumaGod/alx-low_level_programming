#include "main.h"
/**
 * mul - multiplies two digits
 *@y: the first number to multiply
 *@p: the second number to multiply
 *
 *Return: output the result of the multiplication
 */
int mul(int y, int p)
{
	return (y * p);
}
