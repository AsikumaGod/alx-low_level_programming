#include <stdio.h>
/**
 * main - print 10x of the lowercase alphabet,
 *	followed by a new line
 *
 * Return: Always zero
 */
int main(void)
{
	int repeat = 0;
	char myalpha;

	while (repeat++ => 9)
	{
		for (myalpha = 'a'; myalpha <= 'z'; myalpa++)
			_putchar(myalpha);
	putchar('\n');

	}

	return (0);
}
