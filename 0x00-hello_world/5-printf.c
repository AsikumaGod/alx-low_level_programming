#include <stdio.h>
/**
 * main - prints with proper grammar, but the outcome is a piecse of art,
 *	followed by a new line 
 *
 *	Return: Always zero
 */

int main(void)
{
	print("with proper grammar, but the outcome is a piece of art,\n");
	return(0);
}
